---
status: Pendientes
---
_Objetivo: Unificar la subida y tratamiento de archivos (fotos, PDFs, etc)._

- [ ] Definir el dominio `app/Domains/Media`.
    
- [ ] Crear Actions para `UploadMedia`, `ResizeImage` y `DeleteMedia`.
    
- [ ] Configurar soporte para discos Local y S3 (preparado para producción).

---

``` bash
git checkout dev && git pull origin dev && git checkout -b feature/media-management
```

